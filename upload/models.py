#from django.db import models

#class DataSet(models.Model):

    # xxx = models.CharField(max_length=40, blank=False)
    # xxx = models.DateField()
    # xxx = models.ForeignKey(Product, null=False)
    # xxx = models.IntegerField(blank=False)
    # xxx = models.TextField()
    # xxx = models.DecimalField(max_digits=6, decimal_places=2)
    # xxx = models.BooleanField(blank=False, default=False)

    #def __str__(self):
    #    return "{0}-{1}-{2}".format(self.id, self.date, self.full_name)